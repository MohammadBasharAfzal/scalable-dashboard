import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>Navbar</h1>
            {/* Add navbar items here */}
        </nav>
    );
};

export default Navbar;
